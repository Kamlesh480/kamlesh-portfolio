# Copy to .env.local for local development.
# Production value must ALSO be set in the hosting provider's dashboard
# (e.g. Vercel → Project Settings → Environment Variables) — this file
# only affects local dev/build, not the deployed site.
NEXT_PUBLIC_SITE_URL=https://kamlesh-chhipa.com
